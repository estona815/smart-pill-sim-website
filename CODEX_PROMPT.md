# Codex 작업지시서: 스마트 알약/영양제 디스펜서 웹 시뮬레이터

## 1. 프로젝트 개요

한국폴리텍대학 메카트로닉스 융합프로젝트용으로, Raspberry Pi 5 기반 스마트 캡슐 알약 디스펜서의 제작 전 검증 웹 시뮬레이터를 만든다. 시스템은 9인치 HDMI 터치스크린 UI, 서보형 회전 구동부 또는 단순 회전 모듈 기반 슬롯 휠, IR Break Beam 낙하 센서, 알약 저장통, 배출구, 상부 차단판, 3D 프린팅 구조물을 기준으로 한다. PLC는 사용하지 않는다.

현재 설계에서는 외장 모터 드라이버와 별도 원점 보정 센서를 쓰지 않는다. 특정 대형 스텝모터 구성을 실제 하드웨어로 단정하지 말고, 구동부는 슬롯 휠을 일정 각도로 회전시키는 추상 구성으로 다룬다.

웹 시뮬레이터의 목적은 실제 제작 전에 알약 크기, 슬롯 크기, 배출구 폭, 저장통 경사각, 마찰, 동작 속도, 구동각, 센서 위치를 입력하여 정확 배출, 미배출, 중복 배출, 알약 걸림, 센서 감지 실패, 구동 위치 오차를 예측하고, 개선 전/후 결과를 수치와 그래프로 보여주는 것이다.

## 2. 구현 목표

- 사용자가 관련 기입정보를 입력할 수 있는 웹 UI를 만든다.
- 부품 치수와 구비 사항을 입력받는다.
- 제어 로직은 임의 JavaScript 실행이 아니라 안전한 key=value 규칙으로 입력받는다.
- 2D Canvas로 저장통, 회전 슬롯 휠, 배출구, IR 센서, 알약 낙하 장면을 그린다.
- 1회 배출 애니메이션을 제공한다.
- 반복 시뮬레이션을 실행하고 정확 배출률과 실패율을 계산한다.
- 개선 전/후 자동 비교 그래프를 제공한다.
- CSV, 설정 JSON, 장면 PNG 저장 기능을 제공한다.
- 발표자료 캡처에 적합하도록 화면을 깔끔하게 구성한다.

## 3. 기술 스택

- HTML5
- CSS3
- Vanilla JavaScript
- Canvas API
- 외부 라이브러리 사용 금지 또는 최소화
- 인터넷 연결 없이 `index.html`만 열어도 실행 가능하게 작성

## 4. 화면 구성

### 상단 헤더

- 프로젝트 제목
- 핵심 설명
- 예시값 불러오기
- 초기화
- 설정 JSON 저장

### 왼쪽 메인 패널

- 2D 시뮬레이션 Canvas
- 1회 배출 애니메이션 버튼
- 반복 시뮬레이션 버튼
- 개선 전/후 비교 버튼
- CSV 저장 버튼
- 장면 PNG 저장 버튼
- 최근 결과 표시

### 오른쪽 입력 패널

- 프로젝트명
- 구비 부품/요구사항
- 알약 형태, 지름, 길이, 무게, 저장량
- 저장통 경사각, 마찰 계수, 3D 프린팅 공차
- 슬롯 크기, 슬롯 개수, 휠 반지름
- 배출구 폭, 출구 모서리 보정, 상부 차단판
- 구동 방식, 동작 속도, 1회 구동각, 구동각 오차
- 기구 유격/백래시, 전원 안정성 수준
- IR 센서 위치, 감지 범위, 반응 시간, 디바운스 시간
- 목표 배출 수량, 반복 횟수, Seed, 개선 전/후 모드
- 안전한 제어 로직 입력창
- 설정 JSON 입력창

### 하단 결과 패널

- 총 시도 횟수
- 정확 배출률
- 미배출률
- 중복 배출률
- 걸림률
- 센서 감지 실패률
- 평균 배출 시간
- 평균 구동각 오차
- 실패 유형별 막대그래프
- 개선 전/후 정확 배출률 그래프
- 설계 진단표

## 5. 시뮬레이션 규칙

물리엔진이 아니라 규칙·확률 기반 모델로 구현한다.

### 주요 입력값

```js
pillShape
pillDiameter
pillLength
pillWeight
pillCount
hopperAngle
friction
tolerance
slotSize
slotCount
wheelRadius
outletWidth
edgeChamfer
topGuard
driveMode
motorSpeed
motorAngle
actuatorErrorDeg
backlashDeg
powerStability
sensorPosition
sensorRange
sensorResponse
sensorDebounce
targetCount
trialCount
seed
designMode
controlCode
```

### 실패 유형

- `success`: 목표 1개 배출, 센서 1회 감지
- `no_drop`: 슬롯 충전 실패 또는 미배출
- `double_drop`: 실제 2개 이상 배출
- `jam`: 배출구에서 알약 걸림
- `sensor_fail`: 실제 배출은 되었으나 센서 감지 실패
- `motor_error`: 구동각 오차, 기구 유격, 출력 부족, 슬롯 정렬 실패로 인한 배출 위치 불일치

### 확률식 방향

- 슬롯 크기가 알약보다 작거나 너무 가까우면 미배출/걸림 확률 증가
- 슬롯 크기가 알약보다 과도하게 크면 중복 배출 확률 증가
- 배출구 폭이 좁고 마찰이 높으면 걸림 확률 증가
- 동작 속도가 빠르고 가감속이 부족하면 걸림/센서 실패/구동 위치 오차 증가
- 센서 범위가 좁거나 반응 시간이 느리면 감지 실패 증가
- 구동각 오차, 기구 유격, 낮은 전원 안정성은 슬롯 정렬 실패 확률을 증가시킴
- 출구 chamfer와 상부 차단판 적용 시 걸림/중복 배출 감소
- 개선 후 모드는 구조 안정성이 좋아진 것으로 가정하되, 과장하지 말 것

## 6. 안전한 제어 로직 입력

임의 JS 코드를 실행하지 않는다. 아래 key=value 형식만 파싱한다.

```text
retry=1
motorRamp=0.75
doubleDetect=true
sensorCheck=true
alarmOnFail=true
jamRecovery=false
```

- `retry`: 0~3
- `motorRamp`: 0~1
- `doubleDetect`: true/false
- `sensorCheck`: true/false
- `alarmOnFail`: true/false
- `jamRecovery`: true/false

## 7. 파일 구조

```text
smart_pill_sim_website/
├─ index.html
├─ styles.css
├─ app.js
├─ README.md
├─ CODEX_PROMPT.md
└─ GPT_PROMPT.md
```

## 8. 코드 작성 기준

- 모든 기능은 브라우저에서 동작해야 한다.
- 외부 CDN에 의존하지 않는다.
- 함수명은 의미 있게 작성한다.
- 난수 seed를 사용해 결과 재현성을 확보한다.
- Canvas 그래프도 직접 그린다.
- UI는 한국어로 작성한다.
- 발표자료 캡처를 고려해 색과 레이아웃을 과도하게 복잡하게 만들지 않는다.
- 시뮬레이터라는 점을 명확히 하고, 실제 제작 후 실험 보정이 필요하다는 안내를 포함한다.
- 정확 배출률을 조작해서 높이는 방식이 아니라, 구조 개선 조건에 따른 실패 확률 변화를 비교하는 방식으로 설명한다.

## 9. 최종 산출물

- 실행 가능한 정적 웹사이트
- 입력 폼
- 2D 시뮬레이션 Canvas
- 반복 시뮬레이션 계산 로직
- 실패 유형별 그래프
- 개선 전/후 비교 그래프
- CSV 저장 기능
- JSON 저장/불러오기 기능
- PNG 캡처 기능
- README

## 10. 검수 체크리스트

- `index.html`을 더블클릭하면 열리는가?
- 입력값 변경 시 화면이 갱신되는가?
- 1회 배출 애니메이션이 작동하는가?
- 반복 시뮬레이션 결과가 수치로 표시되는가?
- 개선 전/후 그래프가 표시되는가?
- CSV 저장이 되는가?
- JSON 저장/적용이 되는가?
- PNG 저장이 되는가?
- 센서/구동부/슬롯/배출구 조건이 결과에 반영되는가?
- 발표자료에 넣을 수 있을 만큼 화면이 깔끔한가?
