# Handoff Pipeline

이 저장소는 최종 완성품이 아니라, 다른 컴퓨터에서 총괄 작업을 이어가기 위한 러프한 정적 웹앱 틀과 검수 하네스이다. 현재 목표는 긴 토큰 작업을 시작하기 전에 요구사항, 안전 전제, 입력값, 산출물, 검수 기준을 한곳에 고정하는 것이다.

## 현재 범위

- `index.html`, `styles.css`, `app.js`만으로 실행되는 정적 웹 시뮬레이터
- Raspberry Pi 5, MG996R 서보모터, 별도 6V 서보 전원, 토출부 포토센서/IR 기준의 설계 전 검토
- 실패 유형: 정확 배출, 미배출, 중복 배출, 알약 걸림, 센서 감지 실패, 구동 위치 오차
- 실물 테스트 데이터 입력 후 확률식 완만 보정
- 발표/보고서 문장 생성, CSV/JSON/PNG 저장
- 외부 라이브러리 없이 브라우저에서 실행

## 넘김 절차

1. 다른 컴퓨터에서 저장소를 연다.
2. `npm test`를 실행해 정적 검수 하네스가 통과하는지 확인한다.
3. `index.html`을 브라우저로 열어 예시값 불러오기, 반복 시뮬레이션, 개선 전/후 비교, 문장 생성을 확인한다.
4. `GPT_PROMPT.md`와 `CODEX_PROMPT.md`를 총괄 작업자의 장문 프롬프트 시작점으로 사용한다.
5. 실제 치수와 테스트 데이터가 생기면 웹앱 입력값, README, 보고서 문장 생성 결과를 함께 갱신한다.

## 검수 하네스

```bash
npm test
```

검수 스크립트는 다음을 확인한다.

- HTML에서 사용하는 주요 ID와 JavaScript 조회 ID의 불일치
- 과거 구동부/센서 전제 문구의 재등장
- 과장된 성공 보장 표현
- 현재 설계 핵심 문구 포함 여부

GitHub Actions는 `.github/workflows/validate.yml`에서 같은 검수를 수행한다.

## 총괄 작업자에게 넘길 핵심 전제

- Raspberry Pi는 서보모터에 PWM 신호만 전달한다.
- MG996R 전원은 Raspberry Pi에서 직접 공급하지 않는다.
- 24V 파워서플라이를 사용할 경우 Buck 컨버터로 6V / 3A~5A로 낮춘다.
- Raspberry Pi GND와 서보 전원 GND는 공통 연결한다.
- 포토센서/IR은 토출 확인용이며 휠 원점 보정용이 아니다.
- 홀센서와 세밀 분할 회전 제어는 현재 초안 범위에서 제외한다.
- 현재 결과는 실제 성공률 보증이 아니라 제작 전 위험 선별용이다.

## 다음 컴퓨터에서 할 일

- 실제 알약 치수와 슬롯/배출구 설계값 입력
- UI 캡처용 화면 밀도와 모바일 표시 확인
- 확률식 계수 검토 및 실물 테스트 데이터 반영
- 보고서/발표자료 문장 톤 최종 정리
- GitHub Pages 배포 상태 확인
- 필요 시 백엔드나 AI 요약 기능은 별도 브랜치에서 추가 검토

## 넘김 문장

이 프로젝트는 가상 검증을 통해 제작 오차를 줄이는 스마트 알약 자동 분배 시스템의 정적 웹 시뮬레이터 초안이며, 다른 컴퓨터에서 본격 구현과 보고서 정리를 이어가기 위한 하네스 및 파이프라인으로 사용한다.
