# ChatGPT에게 시킬 프롬프트

아래 프롬프트를 그대로 붙여넣으면 실제 치수와 부품 조건을 기준으로 웹 시뮬레이터 개선 방향, Codex 수정 요청, 발표 문장까지 정리할 수 있다.

---

너는 한국폴리텍대학 메카트로닉스 융합프로젝트의 스마트 알약/영양제 디스펜서 제작 코치이자, 웹 기반 가상 시뮬레이션 설계자다.

내 프로젝트는 취약계층용 스마트 캡슐 알약 디스펜서이다. PLC는 사용하지 않고 Raspberry Pi 5 기반 터치 UI와 간단한 서보 구동 제어를 기준으로 한다. 구성은 9인치 HDMI 터치스크린, MG996R 서보모터, 별도 6V 서보 전원, 토출부 포토센서 또는 IR Break Beam 센서, 3D 프린팅 회전 슬롯 휠, 알약 저장통, 배출구, 상부 차단판, 알약 회수 트레이, 부저 또는 LED 알림이다.

Raspberry Pi는 서보에 PWM 신호만 전달한다. MG996R 전원은 Raspberry Pi에서 직접 공급하지 않는다. 24V 파워서플라이를 사용할 경우 DC-DC Buck 컨버터로 6V / 3A~5A로 낮춰 서보에 공급한다. Raspberry Pi GND와 서보 전원 GND는 공통 연결한다.

홀센서는 현재 사용하지 않는다. 포토센서/IR 센서는 알약 배출 여부 확인용이며 휠 기준 위치 확인용이 아니다. 현재 초안은 1회~10회 단기 데모는 가능하지만, 장시간 반복 운용에서는 기준 위치 자동 보정 없음으로 인한 누적 오차 위험이 남는다.

목표 동작은 다음이다.

1. 터치 UI에서 아침/점심/저녁 복용을 선택한다.
2. 선택한 시간대에 맞는 캡슐 또는 알약을 1개 자동 배출한다.
3. 토출부 포토센서/IR로 낙하 여부를 확인한다.
4. 화면에 성공/실패 안내를 표시한다.
5. 미배출, 중복 배출, 알약 걸림, 센서 감지 실패, 구동 위치 오차를 제작 전 웹 시뮬레이션으로 검토한다.

내가 아래에 실제 입력값을 제공할 테니, 다음 결과를 작성해라.

## 입력값

- 알약 형태:
- 알약 지름/폭(mm):
- 알약 길이(mm):
- 알약 무게(g):
- 알약 저장 개수:
- 슬롯 크기(mm):
- 슬롯 개수:
- 회전 휠 반지름(mm):
- 배출구 폭(mm):
- 저장통 경사각(°):
- 마찰 계수 가정:
- 3D 프린팅 공차(±mm):
- 구동부 종류: MG996R 서보모터 / 기타 서보형 회전 구동부
- 동작 속도(rpm):
- 1회 구동각(°):
- 이상적인 슬롯각(°):
- 구동각 오차(±°):
- 기구 유격/백래시(°):
- 서보 토크 여유(%):
- Raspberry Pi 전원:
- 서보모터 전원:
- 24V 파워서플라이 사용 여부:
- Buck 컨버터 출력 전압/전류:
- 공통 GND 연결 여부:
- 원점 보정 방식:
- 포토센서/IR 위치(mm):
- 포토센서/IR 감지 범위(mm):
- 센서 반응 시간(ms):
- 센서 디바운스 시간(ms):
- 재시도 로직:
- 걸림 완화 로직:
- 개선 전/후 비교하고 싶은 조건:

## 요청 결과

1. 현재 입력값 기준으로 예상되는 가장 큰 실패 위험 3가지를 알려줘.
2. 웹 시뮬레이터에서 어떤 값부터 조정해야 하는지 우선순위를 정리해줘.
3. 슬롯 크기, 배출구 폭, 동작 속도, 센서 위치에 대한 추천 범위를 제시해줘.
4. 전원 연결 위험을 냉정하게 판단해줘.
5. 원점 자동 보정 없음이 단기 데모와 장기 운용에 각각 어떤 의미인지 설명해줘.
6. Codex에게 줄 수정 지시문을 작성해줘.
7. 발표자료에 넣을 수 있는 한 문장 요약과 3줄 설명을 작성해줘.
8. 보고서에 넣을 수 있는 문장으로 자연스럽게 정리해줘.
9. 실제 제작 전에 해야 할 테스트 체크리스트를 작성해줘.

문체는 대학생 융합프로젝트 보고서와 발표에 맞게 작성하고, 과장하지 말고 실제 제작 가능한 수준으로 말해라. 최종 메시지는 “가상 검증을 통해 제작 오차를 줄이는 스마트 알약 자동 분배 시스템”으로 정리해라.

---

# 웹 시뮬레이터 수정용 짧은 프롬프트

아래 내용을 Codex에게 전달할 수 있는 짧은 수정 요청으로 바꿔줘.

- 현재 웹 시뮬레이터 기능:
- 부족한 점:
- 추가하고 싶은 입력값:
- 추가하고 싶은 그래프:
- 발표자료에 보여주고 싶은 장면:
- 실제 제작 조건:
- 원하는 산출물:

조건: HTML/CSS/JavaScript 정적 웹앱으로 유지하고, 외부 라이브러리는 사용하지 말 것. 임의 JavaScript 실행 기능은 넣지 말고, 안전한 key=value 입력 방식만 사용할 것.
